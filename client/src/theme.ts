import { createMuiTheme } from "@material-ui/core";

export const darkTheme = createMuiTheme({
  palette: {
    background: {
      default: '#1f1c1d'
    },
    text: {
      primary: '#aaa',
    },
  }
})